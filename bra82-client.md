## 前置条件

仅在 BRA-82 的 UX 子任务完成、其设计核对无阻塞项后开工。以 `docs/shotx-prd-BRA82.md` 和 `docs/shotx-ux-ui-spec-BRA82.md` 为准；后者只补充视觉，不得扩张需求。

## 实现范围

1. 修复 `AnnotationView` / `PinWindowController` 的图像坐标与拖动根因；补最小回归测试。
2. 让现有和新增的矩形、椭圆、画笔等标注对象都可命中、选择、移动、删除、方向键微调与撤销/重做。
3. 重做截图工具栏：无独立移动按钮，使用经核对的 SVG，正确区分矩形、椭圆与滚动截图，并实现设计的分割线和工具专属下拉样式（含文字工具变体）。
4. 按 UX 标注导入/使用录屏设置与录制中菜单 SVG，保持所有覆盖层排除在 ScreenCaptureKit 成片外。
5. 修复扩展显示器选择层鼠标不可达问题（仍限单屏单次截图）。
6. 视频预览的源文件在预览期间外部删除时，关闭/退出必须无阻塞。

## 验收与约束

- 覆盖 `BRA82-01/02/03/04/06/07/08`；在工单评论写明各验收项的代码位置和自动化测试命令结果。
- 不新增依赖或服务端；不修改无关的已有脏工作区变更。
- 对分支/坐标/删除恢复等非平凡逻辑添加最小可运行测试；真实多屏、访达删除和录制 UI 明确列为手测。
