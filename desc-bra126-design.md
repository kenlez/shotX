## 任务

按 BRA-125 PRD（`docs/shotx-prd-BRA125.md`）核对更新设计文件中的蓝色色值，并更新录屏 UI 颜色标注。

## 背景

用户已更新设计文件，要求：截屏/录屏**开始之前**的矩形框与锚点统一为**蓝色**，录屏中框保持**红色**，录屏开始按钮由绿改蓝。此前 BRA-113 设计标注（`docs/shotx-ux-ui-spec-BRA113.md` §6 R3、§9 C1/C2、§11-2）曾以 `record-start.svg`（绿 `#07C160`）为权威并作废蓝色口径——本次按用户新要求，该结论作废。

## 范围

- 从更新设计文件（如仓库 `record-*.svg` / `record-menu.svg` 中蓝色口径）确认「统一蓝色」的色值；默认沿用应用既有蓝 `#10AEFF`（截图选区、`record-menu.svg` 开始按钮矩形均用此值），若设计文件另有明确色值则以设计为准并给出。
- 更新 `docs/shotx-ux-ui-spec-BRA113.md`（或新增 BRA-125 标注小节）：将「绿为权威」「`#10AEFF` 作废」的结论按本次要求反转，明确：
  - 录屏设置态选区边框/锚点 = 蓝；
  - 录屏覆盖层 `.setup`/`.countdown` = 蓝，`.recording` = 红 `#FA5151`；
  - 开始按钮 `record-start.svg` = 蓝。

## 验收标准

1. 给出明确的蓝色色值（含来源：设计文件原值或沿用 `#10AEFF` 的说明）。
2. 标注文档更新完毕，与 BRA-125 PRD 一致；无自创需求变更。

## 仓库上下文

- 仓库 `/Users/brad/Documents/shotX`，分支 `agent/bra-107-font`，与 BRA-116 并行共享工作树。
- 只改文档（`docs/`），不改业务代码。
