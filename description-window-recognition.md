## User request

现在截图对窗口的识别非常不准确，需要建立如下逻辑：

1. 只识别屏幕中可以看到的窗口内容，被完全遮挡的窗口不应被识别。
2. 现在经常因为对大窗口的识别，忽略掉在大窗口之前的小窗口（即窗口重叠时选错了层级）。

## 现状与根因线索

- 窗口模式候选生成：`Sources/ShotX/CaptureCoordinator.swift:278` `captureWindows(in:)` 仅按 `isOnScreen && windowLayer == 0 && 非自身 bundle && 尺寸 > 40×40` 过滤。`isOnScreen` 只表示窗口在屏幕上（未最小化/未离屏），**不排除完全被遮挡的窗口**，因此被完全盖住的窗口仍会进入候选并被高亮/捕获。
- 悬停命中：`Sources/ShotX/CaptureCoordinator.swift:597-604` `mouseMoved` 使用 `windows.first { $0.frame.contains(cgPoint) }`。`SCShareableContent.windows` 的数组顺序**不保证**按前后层级（z-order）排列，因此 `first` 命中的可能是后面的/更大的窗口，导致"大窗口遮挡小窗口"时选错目标。
- 捕获语义不受影响：选中窗口后按 `edit(window:...)` 捕获该窗口 frame 所在屏幕区域；被遮挡不改变目标窗口的完整捕获范围（`docs/shotx-ux-ui-spec.md` §6.3）。本次只改**识别/命中**，不改捕获。

## 范围

- 窗口截图模式 `O-SEL/window` 的候选窗口列表与悬停识别。
- 覆盖：悬停高亮、"这里没有可截取的窗口"兜底、Tab/Shift+Tab 可见窗口切换（若实现）。
- 保留现有过滤：`windowLayer == 0`、排除 ShotX 自身窗口、最小尺寸 > 40×40。

## 用户流程与关键状态

打开窗口截图 → 移动光标 → 只对屏幕上实际可见的窗口给出高亮 → 重叠区域选择最上层可见窗口 → Return/点击捕获。

## 功能要求

- **FR-WIN-01 可见性**：只识别屏幕上可见的窗口内容；完全被遮挡（无任何可见像素区域）的窗口不得进入候选列表，不得被悬停高亮或捕获。
- **FR-WIN-02 层级优先**：光标位于多个窗口重叠区域时，按前后层级（z-order，从前到后）取最上层可见窗口；大窗口在后、小窗口在前时应选小窗口，不得因窗口面积/数组顺序选错。
- 部分遮挡仍可识别：窗口只要存在任何可见区域即可被识别，悬停其可见部分时高亮该窗口。
- 候选窗口按前后层级排序，悬停命中不再依赖 `SCShareableContent.windows` 的数组顺序。

## 关键状态

- 无候选（该屏没有可识别的可见窗口）→ 显示"这里没有可截取的窗口"并提供"切换到区域截图"（沿用现有兜底）。
- 有候选 → 悬停高亮最上层可见窗口。

## 非功能要求

- 不新增第三方依赖；复用 ScreenCaptureKit 与系统窗口 API（如 `CGWindowListCopyWindowInfo` 获取 z-order / bounds）。
- 多显示器坐标换算正确（沿用 `localRect` / `mouseMoved` 的换算口径）。
- 纯逻辑（排序、遮挡判定）需可单测。

## 验收标准

1. 构造"大窗口在下、小窗口在上且两者重叠"的场景：光标停在重叠区域时，高亮并选中**小窗口（上层）**，而不是大窗口。
2. 构造"A 窗口完全盖住 B 窗口"的场景：B 不得被悬停高亮，也不得出现在可捕获候选 / Tab 循环 / 兜底逻辑中。
3. 部分遮挡场景：窗口被部分盖住但仍有可见区域时，悬停其可见区域仍可识别。
4. 现有过滤规则（layer 0、非自身、尺寸阈值）保持有效。
5. `swift build`、`swift test` 通过；新增针对排序/遮挡判定的 XCTest。
6. 真实桌面重叠窗口的手测项明确列出，不把未执行的手测写成通过。

## 非目标

- 不改变选中窗口的**捕获内容范围**（遮挡不裁剪目标窗口，仍捕获完整 frame）。
- 不新增窗口组合截图、不改变标注/录屏。
- 不做服务端，不新增网络能力。
