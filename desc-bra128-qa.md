## 任务

按 BRA-125 PRD（`docs/shotx-prd-BRA125.md`）验收标准独立验收颜色改动，输出验收报告与需求—测试追踪。

## 验收范围（BRA-125 验收标准）

1. 录屏设置态（开始前）选区框与锚点为蓝色，与截图选区蓝色一致。
2. 录屏中边框为红色（未回归）。
3. 录屏开始按钮为蓝色（未开始状态）。
4. `swift test` 全绿；截图选区蓝、`ResultWindow` 保存按钮绿等非目标项未受影响（回归）。
5. 给出通过 / 有条件通过 / 未通过结论，注明手测项与未验证项。

## 方法与约束

- 依据验收标准建立需求—测试追踪（C1–C4 ↔ 验证方式）。
- 优先复用仓库现有测试框架与命令（`swift test`、Makefile）；可执行的自动化项实际运行并记录结果；颜色呈现等需视觉确认的项标为手测/未验证，不得写成通过。
- 不代替开发者自测；回归关注点：截图选区蓝、保存按钮绿、BRA-116 录屏 UI 未回归。

## 仓库上下文

- 仓库 `/Users/brad/Documents/shotX`，分支 `agent/bra-107-font`，工作树含 BRA-102/107/115/116 未提交改动。
- 只读验收 + 报告，不改业务代码。
