# ShotX 用户手测清单（BRA-55，0.1.2 候选）

> 覆盖 PRD v1.7 FR-CAP-14/15/16 及相关回归。下一唯一候选为 0.1.2；当前发布门未通过、尚无正式包体。请仅在收到 0.1.2 后执行，结果填“通过 / 未通过 / 无法测试”。

## 0. 交付信息

| 项 | 内容 |
| --- | --- |
| 交付版本 | `ShotX 0.1.2`（待发布门通过后生成） |
| 包体附件 | 暂无；不得重复使用 0.1.1，待交付 `ShotX-0.1.2.app.zip`、`ShotX-0.1.2-source.zip` |
| 覆盖需求 | FR-CAP-14/15/16（BRA-55） |
| 交付日期 | 2026-08-12 |
| 手测用时预估 | 约 12 分钟 |

## 1. 测试环境（用户填写）

| 项 | 内容 |
| --- | --- |
| Mac 型号 / 芯片 | |
| macOS 版本 | |
| 内建屏分辨率 / 缩放 | |
| 外接显示器数量、分辨率 / 缩放 | |

安装：解压 app 包，将 `ShotX.app` 拖入“应用程序”后打开。

## 2. 固定工具区（FR-CAP-14）

- **TC-ANN-14-01**：区域截图进入标注态，依次切换选择、箭头、矩形、画笔、文字、马赛克、裁剪。期望：工具栏宽高不变，始终保留 7 工具、撤销/重做、样式、复制/保存/分享/贴图/关闭，无行内颜色或大小控件。结果：
- **TC-ANN-14-02**：分别展开/关闭每个带选项工具的浮层。期望：工具栏尺寸和元素不变，其他按钮始终可见可点。结果：
- **TC-ANN-14-03**：在选择/裁剪与带选项工具间切换。期望：样式按钮只改变禁用态/摘要，不增删或改变占位宽度。结果：
- **TC-ANN-14-04**：在最窄可用工作区或低分辨率显示器测试。期望：工具/输出两行重排但元素不减，工具栏完全位于可用工作区。结果：

## 3. 工具选项浮层（FR-CAP-15）

- **TC-ANN-15-01**：从选择工具点击箭头。期望：浮层向下展开于工具栏正下方 6 pt、左对齐；含记忆值、12 色+取色器、滑杆+档位。结果：
- **TC-ANN-15-02**：再点当前工具、点样式按钮、点浮层外、按 Esc、切选择/裁剪。期望：五条关闭路径均关闭浮层，工具栏不变。结果：
- **TC-ANN-15-03**：浮层展开时切换箭头/矩形/画笔/文字/马赛克。期望：位置不变、内容原位更新；马赛克仅有尺寸区。结果：
- **TC-ANN-15-04**：把选区放在屏幕底部再打开浮层。期望：下方不足时翻到上方；上下均不足时贴边，距工作区边缘 ≥8 pt。结果：
- **TC-ANN-15-05**：在矮屏/窄屏和外接显示器分别打开浮层。期望：宽固定 300 pt；过高时内部可滚动；不越屏、不遮挡正在拖动的把手。结果：
- **TC-ANN-15-06**：选预设色并观察色块。期望：选中态同时有 accent 描边和对勾；新标注使用该颜色。结果：
- **TC-ANN-15-07**：点击取色器并选取真实屏幕像素。期望：取色正确，锚点符合 FR-CAP-12，回显与新标注颜色一致。结果：

## 4. 可拖拽笔刷大小（FR-CAP-16）

- **TC-ANN-16-01**：画笔无对象选中时拖动 1–8 pt 滑杆，再画一笔。期望：数值实时更新，下一笔立即使用新粗细。结果：
- **TC-ANN-16-02**：分别测试文字 11–32 pt、马赛克 8–40 px。期望：拖动实时影响下一次绘制/输入，松开才记忆。结果：
- **TC-ANN-16-03**：选中已有箭头/矩形/画笔/文字后拖动滑杆。期望：对象即时反馈；一次拖动只产生一个可撤销样式步骤。结果：
- **TC-ANN-16-04**：点击所有离散档位。期望：滑杆吸附、立即生效并持久化。结果：
- **TC-ANN-16-05**：切换工具、重开截图并重启应用。期望：每个工具独立恢复最近值，不串值。结果：
- **TC-ANN-16-06**：滑杆聚焦后按方向键 / Shift+方向键。期望：分别 ±1 / ±5，边界不越界并持久化。结果：

## 5. 键盘、VoiceOver 与回归

- **TC-A11Y-01**：仅用 Tab/Shift+Tab 打开、遍历并关闭浮层。期望：打开焦点进入浮层，循环覆盖全部控件；关闭回到样式按钮。结果：
- **TC-A11Y-02**：开启 VoiceOver 操作色块、滑杆和档位。期望：色块读出颜色及已选中态；滑杆值随变化更新；控件名称与单位正确。结果：
- **TC-REG-01**：执行区域截图、撤销/重做、复制、保存、分享、贴图、Esc。期望：流程与最近结果语义不变，输出中不含工具栏/浮层。结果：
- **TC-REG-02**：Retina 与双显示器分别截图。期望：输出物理像素、方向、边界不变；浮层使用当前显示器工作区避让。结果：

## 6. 反馈

- 通过数 / 总数：/ 21
- 未通过用例（TC ID、期望、实际、截图/录屏）：
- 无法测试及原因：
- 其他问题：
