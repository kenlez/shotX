## 前置条件

仅在 BRA-82 客户端主任务完成后开工，复用其已确认的截图选区与设计样式。

## 实现范围

将现有手动“采集下一段”滚动截图改为自动采集：当用户指针在已选矩形区域内滚动时，监听真实滚轮事件、进行节流后的连续 ScreenCaptureKit 采样和重叠拼接，并更新实时预览。保留撤销、完成、取消、高度/内存上限；匹配失败、横向偏移或滚动过快必须暂停并保留已拼内容。

不得模拟滚轮、控制第三方窗口或添加第三方依赖。允许基于现有重叠匹配实现最小演进；必须先调研并在评论说明是否有可直接采用的开源实现/系统 API，若没有，说明为何本地帧采样+重叠匹配最合适。

## 验收

覆盖 `BRA82-05`：在静态滚动页中，用户连续滚动不需点击“采集下一段”即可生成无重复长图；异常时清楚暂停。为节流/自动采集状态添加最小测试；真实网页手测步骤和未验证项写入评论。
